---

  - name: Install base packages
    yum: name={{ item }} state=present
    with_items:
      - sysstat
      - htop
      - atop
      - strace
      - lsof
      - ntp
      - ntpdate
      - vim
      - nc
      - tmux
      - wget
      - unzip
      - mlocate
      - git